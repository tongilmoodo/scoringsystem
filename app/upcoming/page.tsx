'use client';

import UpcomingMatchesView from '@/components/UpcomingMatchesView';

// Global /upcoming — auto-detects the active live tournament
export default function UpcomingPage() {
  return <UpcomingMatchesView />;
}
